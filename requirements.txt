# SAREI — full application (backend API + live NeMo ASR).
# From project root with venv activated:
#   python -m pip install -r requirements.txt
#
# Requires Python 3.10 or 3.11 (NeMo may fail on 3.13).
# PyTorch wheels below target NVIDIA GPU + CUDA 12.4.
# CPU-only: comment out the --extra-index-url line and reinstall torch/torchaudio.
#
# Optional ASR training extras: pip install -r requirements-train.txt

# Web API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
pydantic>=2.8.0
pydantic-settings>=2.3.0
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
email-validator>=2.2.0
python-multipart>=0.0.9
httpx>=0.27.0
jsonschema>=4.23.0
python-dotenv>=1.0.0
soundfile>=0.12.1
numpy>=1.24.0

# ASR (PyTorch + NeMo)
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.1.0
torchaudio>=2.1.0
nemo_toolkit[asr]>=2.0.0
librosa>=0.10.0
noisereduce>=3.0.2
